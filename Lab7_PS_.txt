#version 300 es
//#version 450

#ifdef GL_ES
precision highp float;
#endif // GL_ES

layout (location = 0) out vec4 rtFragColor;
// out vec4 rtFragColor;

// VARYING

//PER-VIEW: final color inbound
//in vec4 vColor;

//PeR-FRAGMENT: recieve requirements for final color
in vec4 vNormal;

//in vec2 vTexcoord;
in vec4 vTexcoord;

void main()
{
	//rtFragColor = vec4(0.5, 1.0, 0.0, 1.0);
	
	//PER-VERTEX: just displays the final color from VS
	//rtFragColor = vColor;
	
	//PER-Fragment: calaculate and display
	vec4 N = normalize(vNormal);
	//rtFragColor = vec4(vNormal.xyz * 0.5 + 0.5, 1.0);
	
	rtFragColor = vTexcoord;
}


